#include <stdio.h>
/**
 * main - print numbers of base 10 starting from zero
 * and ends with a new line
 * Return : Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 0; i++)
	{
		printf("%d", i);
	}
	return (0);
}
